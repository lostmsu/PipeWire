namespace PipeWire;

public enum SpaParam: uint {
    INVALID,
    PROP_INFO,
    PROPS,
    ENUM_FORMAT,
    FORMAT,
    BUFFERS,
    META,
    IO,
    ENUM_PROFILE,
    PROFILE,
    ENUM_PORT_CONFIG,
    PORT_CONFIG,
    ENUM_ROUTE,
    ROUTE,
    CONTROL,
    LATENCY,
    PROCESS_LATENCY,
    TAG,
}